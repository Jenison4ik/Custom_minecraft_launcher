# Настройка Nginx с SSL сертификатами

## 🚀 Быстрый старт

### Первый запуск (без SSL сертификатов):

```bash
# 1. Убедитесь, что в .env файле указана переменная DOMAIN
# Например: DOMAIN=jenison.ru

# 2. Запустите nginx и app (nginx автоматически запустится в режиме без HTTPS)
docker-compose up -d nginx app

# 3. Получите SSL сертификаты
docker-compose --profile init run --rm certbot-init

# 4. Перезагрузите nginx (он автоматически переключится на HTTPS)
docker exec nginx_proxy nginx -s reload
```

### После первого запуска:

При последующих перезапусках nginx автоматически обнаружит сертификаты и запустится с HTTPS:

```bash
docker-compose restart nginx
```

## 📋 Структура файлов

- `nginx.conf` - Полная конфигурация с HTTPS (используется когда сертификаты есть)
- `nginx-init.conf.template` - Начальная конфигурация без HTTPS (используется при первом запуске)
- `entrypoint.sh` - Скрипт, который автоматически выбирает нужную конфигурацию
- `SSL_SETUP.md` - Подробное описание всех вариантов решения проблемы SSL

## ⚙️ Переменные окружения

В файле `.env` должны быть определены:

- `DOMAIN` - ваш домен (например: `jenison.ru`)
- `CERTBOT_EMAIL` (опционально) - email для уведомлений Let's Encrypt (по умолчанию: `admin@${DOMAIN}`)

## 🔄 Автоматическое обновление сертификатов

Сервис `certbot` автоматически обновляет сертификаты каждые 12 часов.

## ❓ Часто задаваемые вопросы

**Q: Что делать, если сертификаты не получены?**  
A: Проверьте, что:

- Домен указывает на ваш сервер (DNS настроен правильно)
- Порты 80 и 443 открыты в файрволе
- Nginx запущен и доступен

**Q: Как получить сертификаты заново?**  
A: Удалите старые сертификаты и запустите certbot-init:

```bash
rm -rf ./nginx/certbot/conf/live/${DOMAIN}
docker-compose --profile init run --rm certbot-init
docker exec nginx_proxy nginx -s reload
```

**Q: Как использовать другой email для certbot?**  
A: Добавьте в `.env` файл:

```
CERTBOT_EMAIL=your-email@example.com
```

