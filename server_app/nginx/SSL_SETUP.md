# Решения проблемы "курицы и яйца" с SSL сертификатами

## Проблема

При первом запуске nginx требует SSL сертификаты, но certbot может их получить только когда nginx уже работает и обслуживает challenge-запросы.

## ✅ Реализованное решение: Вариант 1 - Условная конфигурация

### Как это работает:

1. Entrypoint скрипт проверяет наличие сертификатов
2. Если сертификатов нет - использует `nginx-init.conf.template` (без HTTPS, только HTTP на порту 80)
3. Если сертификаты есть - использует `nginx.conf.template` (полная конфигурация с HTTPS)

### Процесс первого запуска:

```bash
# 1. Запустите nginx и app (nginx запустится без HTTPS)
docker-compose up -d nginx app

# 2. Получите сертификаты через certbot-init
docker-compose --profile init run --rm certbot-init

# 3. Перезагрузите nginx (он автоматически переключится на HTTPS конфигурацию)
docker exec nginx_proxy nginx -s reload
```

### Автоматическое переключение при перезапуске:

После получения сертификатов, при следующем перезапуске контейнера nginx автоматически обнаружит сертификаты и переключится на HTTPS конфигурацию.

---

## 🔄 Вариант 2: Самоподписанные сертификаты для первоначального запуска

### Преимущества:

- Nginx всегда запускается с HTTPS
- Не нужно вручную переключать конфигурацию

### Недостатки:

- Пользователи будут видеть предупреждение о самоподписанном сертификате до получения реального
- Нужно генерировать временные сертификаты

### Реализация:

Создайте файл `server_app/nginx/generate-dummy-certs.sh`:

```bash
#!/bin/sh
DOMAIN=${DOMAIN}
CERT_DIR="/etc/letsencrypt/live/${DOMAIN}"

if [ ! -f "${CERT_DIR}/fullchain.pem" ]; then
    echo "Генерация временных самоподписанных сертификатов..."
    mkdir -p "${CERT_DIR}"
    openssl req -x509 -nodes -days 1 -newkey rsa:2048 \
        -keyout "${CERT_DIR}/privkey.pem" \
        -out "${CERT_DIR}/fullchain.pem" \
        -subj "/CN=${DOMAIN}"
fi
```

И обновите entrypoint.sh, добавив вызов этого скрипта перед проверкой.

---

## 🔄 Вариант 3: Отдельный init-контейнер (Docker Compose wait)

### Преимущества:

- Четкое разделение этапов
- Можно использовать стандартные Docker patterns

### Недостатки:

- Требует дополнительных зависимостей или скриптов

### Реализация:

Используйте `docker-compose` с `depends_on` и condition, или init-контейнер для проверки/генерации сертификатов перед запуском nginx.

---

## 🔄 Вариант 4: DNS Challenge вместо HTTP Challenge

### Преимущества:

- Не требует запущенного веб-сервера
- Можно получить сертификаты до запуска nginx

### Недостатки:

- Требует настройки DNS API провайдера
- Более сложная настройка
- Необходимы API ключи DNS провайдера

### Пример для Cloudflare:

```bash
certbot certonly --dns-cloudflare \
  --dns-cloudflare-credentials /path/to/cloudflare.ini \
  -d ${DOMAIN} -d www.${DOMAIN}
```

---

## 🔄 Вариант 5: Ручное получение сертификатов до запуска

### Преимущества:

- Простота
- Полный контроль

### Недостатки:

- Требует ручного вмешательства при первом запуске

### Процесс:

1. Временно запустите простой веб-сервер на порту 80
2. Получите сертификаты через certbot
3. Остановите временный сервер
4. Запустите nginx с полной конфигурацией

---

## Рекомендация

**Используйте Вариант 1** (уже реализован) - он самый универсальный и не требует дополнительных зависимостей. После первоначальной настройки система работает полностью автоматически.
